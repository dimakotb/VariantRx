# API — use /api with Vite proxy, or full URL when backend runs separately
VITE_API_BASE_URL=/api

# true = local mock variants + auth + reports (no MongoDB required)
VITE_USE_MOCK_API=true

VITE_APP_NAME=VariantRx
